import {
  GET_CURRENT_DATE,
  PREV_MONTH,
  NEXT_MONTH,
  JUMP_TO,
  ADD_EVENT,
  EDIT_EVENT,
  DELETE_EVENT,
  SET_LOADING
} from "./types";

export default (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
